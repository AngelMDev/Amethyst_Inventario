<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Spinner
        android:layout_width="252dp"
        android:layout_height="wrap_content"
        android:id="@+id/spinnerSale"
        android:layout_alignParentLeft="true"
        android:prompt="@string/spinner_header"
        android:layout_alignParentStart="true"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/editTextSale"
        android:layout_marginLeft="80dp"
        android:hint="Cantidad"
        android:layout_gravity="right"
        android:layout_alignBottom="@+id/spinnerSale"
        android:layout_toRightOf="@+id/spinnerSale"
        android:layout_toEndOf="@+id/spinnerSale" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:orientation="horizontal">

    </LinearLayout>

</RelativeLayout>